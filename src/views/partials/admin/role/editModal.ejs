<!-- Logout Modal-->
<div class="modal fade" id="editModal-<%= role.id %>" tabindex="-1" role="dialog" aria-labelledby="editModalLabel"
    aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Edit Role</h5>
                <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div class="modal-body">
                <form>
                    <div class="form-group">
                        <label for="exampleFormControlInput1">
                            Name
                        </label>
                        <input type="text" value="<%= role.name %>" class="form-control" id="name-<%= role.id %>"
                            placeholder="">
                    </div>
                </form>
            </div>
            <div class="modal-footer">

                <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
                <div class="spinner-border text-primary" id="loading-edit-<%= role.id %>" role="status" hidden>
                    <span class="sr-only">Loading...</span>
                </div>
                <button class="btn btn-primary" id="buttonEdit-<%= role.id %>"
                    onclick="editData('<%= role.id %>', '<%= appLink %>')">Edit</button>
            </div>
        </div>
    </div>
</div>